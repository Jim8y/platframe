section(class="canvas section-1" id="section-1")

    h2 StealthMode

    div(class="content")

        div

            img(src="images/home/section-1/_inline/structure.png" inline)

            article

                h2 System Structure

                p.
                     The system structure of our PDE system. The user
                     P has two keys, the true key Kt to encrypt and decrypt the
                     sensitive data 𝑚 in the Trusted OS, and the decoy key Kd to
                     deal with the decoy data 𝑚′ in the Rich OS. The flash has two
                     mode, the public mode and the hidden mode.

    +nav-1({ target: "section-2", direction: "next" })
